// Spinner.js
import React from 'react';
import './Spinner.css'; // Create some basic CSS for spinner

const Spinner = () => {
    return <div className="spinner">Loading...</div>;
};

export default Spinner;
